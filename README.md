# Real-Time Chat Application

## Project Overview
This project is a real-time chat application built with Node.js, Express, Socket.io, and React. Users can join a global chat, see online users, send messages, and see typing indicators.

## Setup Instructions
### Server
1. Navigate to `server` folder
2. Install dependencies:
   ```bash
   npm install
